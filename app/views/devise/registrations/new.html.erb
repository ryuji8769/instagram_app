<div class="conteiner">
  <div class="sing_up">
    <h1>Instagram</h1>
    <p>登録して友達の写真や動画を<br>
        チェックしよう
    </p>
<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

    <div class="field1">
      <%= f.email_field :email, autofocus: true, autocomplete: "email" ,placeholder: "電話番号、ユーザーネーム、メールアドレス" %>
    </div>

    <div class="field1">
      <%= f.text_field :name ,placeholder: "フルネーム" %>
    </div>

    <div class="field1">
      <%= f.text_field :nickname, placeholder: "ユーザ一ネーム" %>
    </div>

    <div class="field1">
      <%= f.password_field :password, autocomplete: "new-password" ,placeholder: "パスワード" %>
    </div>

    <div class="actions1">
      <%= f.submit "登録する" %>
    </div>

    <div class="sing_up1">
      <p>登録することで、Facebookの利用規約、データ<br>
      に関するポリシー、Cookieポリシーに同意する<br>
　　　　　　　ものとします。</p>
    </div>
  </div>

  <div class="actions">
  <p>アカウントをお持ちでないですか？</p>
    <%= f.submit "登録する" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
</div>